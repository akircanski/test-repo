name: 'My JavaScript Action'
    description: 'A custom action written in JavaScript'
    inputs:
      my-input:
        description: 'An example input'
        required: true
    outputs:
      my-output:
        description: 'An example output'
    runs:
      using: 'node20' # Or 'node20' for newer versions
      main: 'dist/index.js' # Or 'src/index.js' if not compiled
